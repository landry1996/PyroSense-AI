spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pyrosense_ingestion
    username: pyrosense
    password: pyrosense
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379

pyrosense:
  mqtt:
    broker-url: tcp://localhost:1883
    enabled: true
  device-service:
    url: http://localhost:8082

logging:
  level:
    com.pyrosense.ingestion: DEBUG
